// Expressions 

2 + 2   // производит значение
37  // значение
true && false


// Statements

const number = 10;

if(number > 0) {
   const text = 'The number is positive'; // Statement - как законченное предложение
}


console.log(`${number - 2} is also positive number.`) // ${} - ожидаем получить значение(Expression) поэтому туда нельзя вставить Statement if