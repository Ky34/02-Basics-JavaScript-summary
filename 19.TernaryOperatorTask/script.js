const age = prompt('Enter your age');
const alertMessage = age >= 18 ? 'You can buy the alcohol.' : 'You can not buy the alcohol, you are too young!';

console.log(alertMessage);
alert(alertMessage);
